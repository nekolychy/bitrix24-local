import {SkuResourcesEditor} from '../../src/sku-resources-editor';

describe('SkuResourcesEditor', () => {
	it('Should be a function', () => {
		assert(typeof SkuResourcesEditor === 'function');
	});
});
