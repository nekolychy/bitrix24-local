import {OnlyofficePromoActions} from '../../src/onlyoffice-promo-actions';

describe('OnlyofficePromoActions', () => {
	it('Should be a function', () => {
		assert(typeof OnlyofficePromoActions === 'function');
	});
});
